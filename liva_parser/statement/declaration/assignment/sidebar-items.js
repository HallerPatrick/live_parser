initSidebarItems({"struct":[["Assignment","A assignment is a statement, while a re-assignemnt is an epression so we only parse a statement here, therefore is the let mandatory"],["LAssignment","A assignment is a statement, while a re-assignemnt is an epression so we only parse a statement here, therefore is the let mandatory"]]});