use crate::parser::Res;

struct Function {
    name: String,
}

// This helps
use nom::sequence::tuple;

// For parsing if-expressions and func parameters
use nom::sequence::delimited;

// fn parse_function(input: &str) -> Res<&str, Function> {

// }
